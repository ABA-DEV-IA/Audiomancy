import { RuntimeConfig } from "@/config/settings";

export const API_PLAYLIST_URL = `${RuntimeConfig.NEXT_PUBLIC_BACKEND_URL}/jamendo/tracks`;
export const API_PLAYLIST_GENERATE_URL = `${RuntimeConfig.NEXT_PUBLIC_BACKEND_URL}/generate/playlist`;
